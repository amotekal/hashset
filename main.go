package main

import (
	"fmt"
	"hashset/hashset"
)

// type as struct {
// 	i int
// }

// type bs struct {
// 	i int
// }

func main() {
	p := hashset.Hashset{}

	// var m = make(map[int]interface{})
	// a := as{1}
	// b := bs{2}

	// m[1] = a
	// m[2] = b
	// fmt.Print(m)

	fmt.Print(p)
}
